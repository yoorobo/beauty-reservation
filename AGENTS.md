# AGENTS.md — beauty-reservation 하네스 운영 규약

이 저장소는 다중 에이전트 HARNESS로 운영된다. 모든 작업은 **WO(Work Order)** 단위로
발행·수행·검증되며, 각 에이전트는 아래 역할과 보고 규칙을 따른다.

## 1. 역할 (Roles)

| 에이전트 | 역할 | 책임 요약 |
|---|---|---|
| **정학** | Owner / 승인권자(PM) | WO 발행, 계획 승인, 차이 게이트 최종 승인, push 인가. 유일한 위험명령 인가자. |
| **클로** | Orchestrator | WO 분해·배정, 에이전트 간 조율, 산출물 통합, 충돌 조정. |
| **레오** | Executor (구현) | WO STEP을 명시 명령으로 실행. STEP별 증거 첨부. 중단 게이트 준수. |
| **도우** | Investigator (조사) | 인벤토리·리서치·문서 정리. 코드 변경 없이 사실 수집·보고. |
| **차이** | Reviewer (차이 게이트) | 계획 대비 산출물 차이 검증. 비밀 스캔·SR 역참조·DoD 판정. 통과/보류 판정. |

## 2. 보고 규칙 (Reporting)

- **이해 요약 + 계획 선행**: WO 착수 전 이해 요약과 STEP 계획을 출력하고 정학 승인을 대기한다.
- **명시 명령만**: 지시되지 않은 임의 작업 금지. 범위 확대 필요 시 보고 후 승인받는다.
- **STEP별 증거**: 각 STEP은 실행 명령과 출력(해시·카운트·로그)을 증거로 첨부한다.
- **SR ID 역참조**: 구현·설계·커밋 메시지는 근거 SR/NFR ID를 명시한다.
- **중단 게이트 정지**: WO에 명시된 게이트에서 정지하고, push는 승인 후에만 수행한다.
- **충돌 사전 보고**: 병렬 WO와 파일·리소스 충돌 우려 시 착수 전 보고한다.
- **완료 보고**: DoD 체크리스트 판정 + 보고서 파일 저장.

## 3. 하네스 사이클 (Cycle)

```
WO 발행(정학)
  → 이해 요약 + 계획(레오/도우)
  → 계획 승인(정학)
  → STEP 실행 + 증거(레오)
  → 중단 게이트 정지
  → 차이 게이트 검증(차이): 비밀 스캔 · SR 역참조 · DoD
  → 최종 승인(정학)
  → push / 완료 + 보고서
```

## 4. 위험 명령 정책

- `git push`, `delete`(파일/브랜치/리모트), `--force` 계열은 **위험 명령**이다.
- 위험 명령은 **WO 명시 + 차이 게이트 통과 + 정학 승인** 3요건을 모두 충족해야 실행한다.
- 스테이징은 파일별 명시(`git add .` 금지). 비밀은 3층 스캔(추적/미추적/인덱스)으로 차단한다.

## 5. 정본(正本) 원천

- **SR 정본**: `docs/requirements/`
- **설계 문서**: `docs/design-docs/` (IS 명세, 구현·TDD 계획, 컴포넌트 다이어그램)
- **실행 계획**: `docs/exec-plans/`
- 상충 시 `docs/requirements/`의 SR이 우선한다.
