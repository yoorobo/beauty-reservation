# [WO-0729-17] 8회차 — Spring Report API 소비 설정
# 이 파일은 커밋 대상(자리표시자만). 실제 값은 .env 에 넣으며 .env 는 커밋하지 않는다.

# Spring 서버 주소 (끝에 슬래시 없이)
API_BASE_URL=http://localhost:8080

# Spring 실행구성의 환경변수 API_KEY 와 반드시 동일한 값
API_KEY=여기에_스프링과_동일한_키
